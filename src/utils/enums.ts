export enum routes {
  home = "/",
  admin = "/admin",
  blog = "/blog",
  images = "/images",
  user = "/user",
  signup = "/api/auth/signup",
  login = "/api/auth/login",
  logout = "/api/auth/logout",
}

export enum userRole {
  admin = "admin",
}
