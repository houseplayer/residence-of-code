export enum routes {
	home = "/",
	admin = "/admin",
	blog = "/blog",
	images = "/images",
	user = "/user",
	signup = "/signup",
	login = "/login",
	logout = "/logout",
}

export enum userRole {
	user = "user",
	admin = "admin",
}
